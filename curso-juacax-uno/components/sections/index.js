export default function Section({titulo}){
    return(
        <section id="inicio">
            <h2>{titulo}</h2>
            
            <p>Estas viendo la seccion {titulo}.</p>

      </section>
    )
}